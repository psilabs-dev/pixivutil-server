## pixivutil-server-common

Shared models and type contracts for PixivUtil Server packages.

This package contains DTOs and type aliases used by both:
- `pixivutil-server` (server)
- `pixivutil-server-client` (Python client SDK)

It must remain free of PixivUtil2 runtime imports.
